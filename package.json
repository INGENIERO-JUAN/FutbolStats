{
  "name": "futbol-stats-pro",
  "version": "1.0.0",
  "description": "API para gestión de tablas de posiciones de fútbol",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "test": "cross-env NODE_ENV=test DATABASE_URL=postgresql://postgres:password123@localhost:5432/futbol_db jest --runInBand --detectOpenHandles"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^30.4.2",
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
